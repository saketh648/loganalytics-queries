import sqlite3
import random
import json
import base64
import hashlib
from http.server import HTTPServer, SimpleHTTPRequestHandler
from datetime import datetime

DB_NAME = "dashboard.db"

# Helper function to hash passwords using built-in hashlib
def hash_password(password):
    return hashlib.sha256(password.encode('utf-8')).hexdigest()

# 1. Initialize SQLite Database with Users table
def init_db():
    conn = sqlite3.connect(DB_NAME)
    cursor = conn.cursor()
    
    # Create metrics table
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS metrics (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            category TEXT NOT NULL,
            value INTEGER NOT NULL,
            updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )
    ''')
    
    # Create users table
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS users (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            username TEXT UNIQUE NOT NULL,
            password_hash TEXT NOT NULL
        )
    ''')
    
    # Insert default admin user if missing (Username: admin, Password: adminpassword)
    cursor.execute('SELECT COUNT(*) FROM users')
    if cursor.fetchone()[0] == 0:
        default_hash = hash_password("adminpassword")
        cursor.execute('INSERT INTO users (username, password_hash) VALUES (?, ?)', ('admin', default_hash))
        print("Created default user: admin / adminpassword", flush=True)

    # Insert sample metrics if empty
    cursor.execute('SELECT COUNT(*) FROM metrics')
    if cursor.fetchone()[0] == 0:
        sample_data = [('Sales', 120), ('Marketing', 85), ('Engineering', 150), ('Support', 65)]
        cursor.executemany('INSERT INTO metrics (category, value) VALUES (?, ?)', sample_data)
        
    conn.commit()
    conn.close()

# 2. Custom HTTP Handler with Basic Authentication
class AuthenticatedDashboardHandler(SimpleHTTPRequestHandler):

    def authenticate(self):
        auth_header = self.headers.get('Authorization')
        if not auth_header:
            return False

        try:
            # Extract credentials from 'Basic <base64>'
            auth_type, encoded = auth_header.split(' ', 1)
            if auth_type.lower() != 'basic':
                return False

            decoded = base64.b64decode(encoded).decode('utf-8')
            username, password = decoded.split(':', 1)

            # Check credentials against SQLite database
            conn = sqlite3.connect(DB_NAME)
            cursor = conn.cursor()
            cursor.execute('SELECT password_hash FROM users WHERE username = ?', (username,))
            row = cursor.fetchone()
            conn.close()

            if row and row[0] == hash_password(password):
                return True
        except Exception as e:
            return False

        return False

    def send_auth_prompt(self):
        self.send_response(401)
        self.send_header('WWW-Authenticate', 'Basic realm="Dashboard Login"')
        self.send_header('Content-type', 'text/html')
        self.end_headers()
        self.wfile.write(b"401 Unauthorized: Access denied. Please log in.")

    # Intercept all GET requests
    def do_GET(self):
        if not self.authenticate():
            self.send_auth_prompt()
            return

        if self.path == '/api/metrics':
            self.send_response(200)
            self.send_header('Content-type', 'application/json')
            self.end_headers()

            conn = sqlite3.connect(DB_NAME)
            cursor = conn.cursor()
            cursor.execute('SELECT category, value, updated_at FROM metrics')
            rows = cursor.fetchall()
            conn.close()

            data = [{"category": row[0], "value": row[1], "updated_at": row[2]} for row in rows]
            response = {"timestamp": datetime.now().strftime("%I:%M:%S %p"), "data": data}
            self.wfile.write(json.dumps(response).encode('utf-8'))
        else:
            if self.path == '/':
                self.path = '/index.html'
            return super().do_GET()

    # Intercept all POST requests
    def do_POST(self):
        if not self.authenticate():
            self.send_auth_prompt()
            return

        if self.path == '/api/simulate-update':
            conn = sqlite3.connect(DB_NAME)
            cursor = conn.cursor()
            cursor.execute('SELECT id, value FROM metrics')
            rows = cursor.fetchall()

            for row in rows:
                random_change = random.randint(-10, 20)
                new_val = max(10, row[1] + random_change)
                cursor.execute('UPDATE metrics SET value = ? WHERE id = ?', (new_val, row[0]))

            conn.commit()
            conn.close()

            self.send_response(200)
            self.send_header('Content-type', 'application/json')
            self.end_headers()
            self.wfile.write(json.dumps({"status": "success"}).encode('utf-8'))

if __name__ == '__main__':
    init_db()
    server_address = ('', 8000)
    httpd = HTTPServer(server_address, AuthenticatedDashboardHandler)
    print("Server running at http://localhost:8000 ... Press Ctrl+C to stop.", flush=True)
    httpd.serve_forever()
